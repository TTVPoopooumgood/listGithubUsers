{
    "name": "github-user-search",
    "version": "1.0.0",
    "description": "GitHub User Search",
    "scripts": {
        "build": "echo 'No build step required for static HTML/CSS/JS site'",
        "predeploy": "npm install",
        "deploy": "gh-pages -d ."
    },
    "devDependencies": {
        "gh-pages": "^6.0.0"
    }
}
